const params = new URLSearchParams(window.location.search);
const showImage = params.get('item');

    
const detailContent = document.querySelector('.detailContent');
    
if(detailContent){
    if (showImage === 'graphicyang') {
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicyangdetail.png';  // uxport.html 기준 상대 경로
        imgElement.alt = 'yang Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }
    }
    else if (showImage === 'graphicum'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicumdetail.png';  
        imgElement.alt = 'um Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphicssbook'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicssbookdetail.png';  
        imgElement.alt = 'ssbook Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphicsnu'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicsnudetail.png';  
        imgElement.alt = 'snu Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphicrui'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicruidetail.png';  
        imgElement.alt = 'rui Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphicmarry'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphicmarrydetail.png';  
        imgElement.alt = 'marry Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphickwposter'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphickwposterdetail.png';  
        imgElement.alt = 'kwposter Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphickwle'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphickwledetail.png';  
        imgElement.alt = 'kwle Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphickwbook'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphickwbookdetail.png';  
        imgElement.alt = 'kwbook Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphichibrew'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphichibrewdetail.png';  
        imgElement.alt = 'hibrew Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphichibrewposter'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphichibrewposterdetail.png';  
        imgElement.alt = 'hibrewposter Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
    else if (showImage === 'graphichanam'){
        // 첫 번째 이미지 생성 및 설정
        const detailContent = document.querySelector('.detailContent');
        const imgElement = document.createElement('img');

        // 삽입할 PNG 이미지의 경로 설정
        imgElement.src = './image/graphichanamdetail.png';  
        imgElement.alt = 'hanam Image';
        imgElement.width = 700;  // 필요에 따라 크기 조정
        
  

        // .detailContent에 이미지 추가
        if (detailContent) {
            detailContent.appendChild(imgElement);
        }        
    }
}
    